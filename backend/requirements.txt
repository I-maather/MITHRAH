fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2
SQLAlchemy>=2.0
alembic>=1.13
APScheduler>=3.10
pytest>=8.0
httpx>=0.27
# مطلوبة لتشفير كلمة مرور المفتاح (RSA/PKCS1) قبل إرسالها إلى
# POST /session. بدونها يفشل تسجيل الدخول بـCapitalAuthError.
cryptography>=42
# توليد رمز QR للاقتران في الطرفية. نقيّة بلا اعتماديات.
segno>=1.6
