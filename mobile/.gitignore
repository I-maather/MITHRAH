node_modules/
.expo/
dist/
web-build/
coverage/

# native projects are generated by `expo prebuild` — never committed
ios/
android/

# secrets — only .env.example is tracked, and it holds placeholders only
.env
.env.*
!.env.example

*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Apple signing material must never enter this repo
*.p8
*.p12
*.mobileprovision
*.cer
*.certSigningRequest

.DS_Store
*.tsbuildinfo
